import clsx from 'clsx';

const Navigation_alt = ({ router, className }) => {
  return (
    <nav className={clsx('text-base lg:text-sm', className)}>
      <ul role='list' className='space-y-9'>
        <li>Data Navigation will be here</li>
      </ul>
    </nav>
  );
};

export default Navigation_alt;
